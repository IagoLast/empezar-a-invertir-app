{
  "name": "empezar-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "dev": "node --env-file-if-exists=.env.local dev.js"
  }
}
